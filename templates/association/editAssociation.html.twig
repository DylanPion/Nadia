{% extends 'base.html.twig' %}

{% block title %}
	Modifier l'Association

{% endblock %}

{% block body %}
	{% include "_sidebar.html.twig" %}

	<div class="form">
		<div class="form-content">
			<h1>Modification de l'association :
				{{associationName}}</h1>
			{{form_start(formView)}}
			{{form_row(formView.Name)}}
			{{form_row(formView.Button) }}
			{{form_end(formView)}}
		</div>
	</div>
{% endblock %}
